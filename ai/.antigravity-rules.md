# Antigravity Rules for Dwarik-Door

1. **Always Consult Graphify Outputs First**: Before attempting to read application logic, routing, or database structure, you MUST read `ai/graph.json` or `ai/GRAPH_REPORT.md`.
2. **Use Graph Relationships Before Scanning Files**: Rely on the relationships defined in the graph to navigate the codebase instead of running global `grep` or `list_dir` commands unnecessarily.
3. **Query Graph Nodes Before Loading Raw Source Code**: Identify the specific files/nodes that contain the answer to the user's query.
4. **Load Only Referenced Files**: When you need implementation details, read ONLY the files specifically referenced by the graph nodes.
5. **Minimize Token Consumption**: Avoid repository-wide searches unless the graph lookup fails or the graph is significantly out of date. Use graph navigation whenever possible.
